name = "ret2text"
version = "1.0.0"
author = "fb0sh@outlook.com"
category = "pwn"
description = "return to text segment"

# Optional: 显式 safe_name；缺省由 name 派生（派生失败时必须显式提供）
# safe_name = "xxx"

attachment = "attachment/ret2text"

[flag]
type = "dynamic"

[docker]
port = 1337

[docker.recommended_resources]
cpu_millis = 500
memory_bytes = 268435456
pids_limit = 100
